# 신본관 회의실1 기존 Excel 분석

분석 대상은 로컬 원본 1개이며 원본과 실명 데이터는 저장소에 포함하지 않았습니다.

## A. 확인된 사실

- 워크시트는 1개이며 이름은 `2024`, used range는 `A1:AC19`입니다.
- 눈금선은 숨김이며 인쇄는 A4 가로, 배율 84%, 좌우 여백 0.25인치입니다.
- 열 너비는 A:B 1.21875, C 3.33203125, D:AB 5.109375, AC 8입니다.
- 핵심 행 높이는 3행 36pt, 6행 129.75pt, 12행 130.5pt, 14행 36pt입니다. 18행은 숨김입니다.
- 병합은 `P6:Q6` 한 곳입니다. 이 셀은 연한 강조색, 굵은 테두리와 굵은 글꼴을 사용합니다.
- 상단 장변 `D6:AB6`는 25개 열 폭을 사용하지만 `P6:Q6` 병합 때문에 24개 좌석 위치로 읽힙니다.
- 하단 장변 `D12:AB12`는 25개 개별 좌석 위치입니다.
- `S3`, `U3`, `W3`, `Y3`은 테두리 사각형이며 모두 `배석`이라고 명시되어 있습니다.
- 오른쪽에 `화면`(`AC8`), `단상`(`AB10`), 아래쪽에 `PC`(`AB14`)가 표시됩니다.
- 아래쪽 중앙에 `내측`(`N14`)이 표시됩니다.
- 좌측 `C7/C9/C11`과 아래쪽 `S14/U14`에는 무표기 사각형이 있습니다.
- 아래쪽 `D15:E15`, `Y15:Z15` 부근에는 대각선이 포함된 개구부 모양이 있습니다.
- 492개 셀이 서식을 가지며 값이 있는 셀은 22개뿐입니다. 빈 셀 서식이 배치도 구조의 대부분을 구성합니다.

## B. 높은 확률로 읽을 수 있는 구조

- 상단 24석과 하단 25석은 긴 테이블 양쪽의 메인 좌석군입니다.
- `P6:Q6`은 일반 좌석보다 넓고 강조되어 있어 중심 또는 중요 좌석으로 사용된 위치일 가능성이 높습니다. 앱에서는 `SEOUL-UPPER-13` 강조석 후보로만 표시하고 공식 상석으로 확정하지 않습니다.
- 기존 배치는 강조 위치 주변에 참석자를 두고 인접 좌석으로 확장한 흔적이 있습니다. 자동배치는 사용자가 기관별 기준석과 숫자 순위를 지정한 뒤 오른쪽, 왼쪽 순으로 교차 확장합니다.
- `배석` 4개는 메인석과 다른 별도 좌석군으로 관리하는 것이 안전합니다.
- 화면과 단상이 오른쪽에 있으므로 웹 좌표도 같은 상대 방향을 유지합니다.

## C. Excel만으로 확인할 수 없는 부분

- 실제 방의 가로·세로 치수와 정확한 축척
- 문과 창의 실제 위치·개수·여닫이 방향
- 긴 테이블의 실제 폭·형상·곡률과 의자 방향
- 좌측 무표기 사각형 3개와 하단 무표기 사각형 2개의 용도
- 대각선 개구부 모양이 실제 문인지, 가구 또는 표기 관행인지 여부
- 각 좌석의 모니터 유무

이 정보는 시제품의 좌석 배정 기능을 막지 않지만, 실제 공간과 일치하는 Production 평면도를 확정하려면 현장 자료가 필요합니다.

## 추가 현장자료

- 입구 반대편에서 회의실 전체가 보이도록 촬영한 사진 1장
- 화면을 정면으로 보고 좌·우 벽과 긴 테이블이 함께 보이는 사진 각 1장
- 출입문과 창 위치가 보이는 사진 각 1장
- 좌측 무표기 사각형 3개, 하단 무표기 사각형 2개의 용도를 확인할 수 있는 근접 사진
- 방 전체 가로·세로, 긴 테이블 전체 길이·폭, 화면·단상·PC의 벽 기준 거리

